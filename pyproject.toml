[project]
name = "model-runner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "torch>=2.8.0",
    "torchvision>=0.22.0",
    "flask>=3.1.2",
    "flask-cors>=6.0.1",
    "setuptools>=80.9.0"
]

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "torch>=2.8.0",
    "torchvision>=0.22.0",
]
build-backend = "setuptools.build_meta"