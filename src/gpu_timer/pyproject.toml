[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "torch>=2.5.1",
]
build-backend = "setuptools.build_meta"

[project]
name = "gpu_timer_cpp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.5.1"
]
